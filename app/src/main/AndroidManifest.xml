<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.messenger_app">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Messenger_App"
        tools:targetApi="n">
        <activity android:name=".ui.AddFriendsUI"
            android:label="Add Friends"/>
        <activity
            android:name=".ui.MessagesUI"
            android:label="Messages" />
        <activity
            android:name=".ui.FriendListUI"
            android:label="Friend List" />
        <activity
            android:name=".ui.LoginUI"
            android:label="Login" />
        <activity
            android:name=".ui.RegisterUI"
            android:label="Register" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>